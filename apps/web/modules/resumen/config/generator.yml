generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Resumen
    theme:                 jroller
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          resumen
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:
        id:    { label: Numero }
        fecha: { date_format: dd/MM/yyyy }
        lista_id: { label: Lista Precio }
        nro_factura: { label: Numero }
        tipofactura_id: { label: Factura }
        tipo_venta_id: { label: Tipo Venta }
        remito_id: { label: Remito }
      list:
        max_per_page: 10
        table_method: retrieveConJoins
        title: Ventas realizadas
        display: [TipoFactura, TipoVenta, nro_factura, fecha, Cliente, _total, _cae_afip, Usuario]
        object_actions:
          _delete: {label: Eliminar}
          _edit: {label: Modificar}
          detalle: {label: Detalle}
          mail: {label: Email}
          factura: {label: Obtener CAE}
        batch_actions:
          _delete: {label: Eliminar}
      filter: ~
      form:
        display: [tipofactura_id, tipo_venta_id, fecha, cliente_id, lista_id, remito_id, nro_factura, cuit, afip, saldo_pesos, saldo_dolar, observacion]
        actions:
          _list: {label: Volver}
          _save: {label: 'Guardar y Agregar Detalle'}
      edit:
        title: Modificar Venta
      new:
        title: Nueva Venta
